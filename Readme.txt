CK-12 Path Optimizer.

* This project has no dependency on any third party library or framework.
* To execute the application, just launch the browser from "ck-12.html" file.
* Please don't change the project structure, otherwise the app might now work.
* Graph input is already set, so no input is required to run the app.
